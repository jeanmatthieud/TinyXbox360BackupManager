// SPDX-FileCopyrightText: 2026 Manuel Quarneti <mq1@ik.me> (TinyWiiBackupManager)
// SPDX-FileContributor: Modified by Jean-Matthieu Dechriste (TinyXbox360BackupManager)
// SPDX-License-Identifier: GPL-3.0-only

export enum Page {
    Games,
    Toolbox,
    ConversionQueue,
    Settings,
    About,
}

export struct DisplayedConfig {
    path: string,
    // Cible affichée : chemin local ou ftp://ip. Vide si aucune cible.
    target: string,
    target-kind: string,
    mount-point: string,
    remove-sources-games: string,
    sort-by: string,
    view-as: string,
    theme-preference: string,
    show-x360: bool,
    show-og: bool,
    console-ip: string,
    ftp-port: string,
    ftp-user: string,
    ftp-password: string,
}

export struct DisplayedGame {
    path: string,
    is-x360: bool,
    format: string,
    size-gib: float,
    title: string,
    id: string,
    cover: image,
}

export struct DisplayedDriveInfo {
    label: string,
    used-gib: float,
    games-gib: float,
    total-gib: float,
    fs-kind: string,
    allocation-granularity: int,
}

export struct Notification {
    text: string,
    critical: bool,
}
