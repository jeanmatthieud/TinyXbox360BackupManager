// SPDX-FileCopyrightText: 2026 Manuel Quarneti <mq1@ik.me> (TinyWiiBackupManager)
// SPDX-FileContributor: Modified by Jean-Matthieu Dechriste (TinyXbox360BackupManager)
// SPDX-License-Identifier: GPL-3.0-only

import { GamesToolbar } from "components/games-toolbar.slint";
import { ScrollView } from "std-widgets.slint";
import { HorizontalLine } from "components/horizontal-line.slint";
import { IconSet, IconDisplay } from "@lucide";
import { Card } from "components/card.slint";
import { MyPalette } from "styling.slint";
import { GameRow } from "components/game-row.slint";
import { GameTableHeader } from "components/game-table-header.slint";
import { GamesOtherToolbar } from "components/games-other-toolbar.slint";
import { UiState } from "ui-state.slint";
import { DisplayedGame } from "types.slint";
import { GamesPlaceholder } from "components/games-placeholder.slint";

export component GameTablePage inherits VerticalLayout {
    VerticalLayout {
        padding: 10px;
        spacing: 10px;

        GamesToolbar { }

        // Same rule as the grid: kept whenever the library holds games, so
        // the search box stays reachable when the filter matches nothing.
        if !UiState.config.target.is-empty && UiState.has-games: GamesOtherToolbar { }
    }

    if UiState.config.target.is-empty || UiState.games.length == 0: GamesPlaceholder { }

    if !UiState.config.target.is-empty && UiState.games.length > 0: ScrollView {
        width: parent.width;
        viewport-height: v.preferred-height;

        v := VerticalLayout {
            padding: 10px;
            padding-top: 0px;

            Card {
                clip: true;
                height: v2.preferred-height;

                v2 := VerticalLayout {
                    GameTableHeader { }

                    HorizontalLine { }

                    for game[i] in UiState.games: VerticalLayout {
                        GameRow {
                            game: game;
                        }

                        HorizontalLine { }
                    }
                }
            }
        }
    }
}
